{
  "name": "ui5-speech-app",
  "version": "1.0.0",
  "private": true,
  "description": "OpenUI5 app that streams speech input into an Input and commits to a List after a silence threshold.",
  "scripts": {
    "start": "ui5 serve --open index.html --config ui5.yaml",
    "build": "ui5 build --all --clean-dest",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@ui5/cli": "^3.0.0",
    "rimraf": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "ui5": {
    "dependencies": []
  },
  "license": "MIT",
  "author": ""
}